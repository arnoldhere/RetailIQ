# Server environment variables example
PORT=8888
HOST=localhost

# MySQL (SQL) configuration
DB_HOST_SQL=127.0.0.1
DB_USER_SQL=retail
DB_PASSWORD_SQL=retailpass
DB_NAME_SQL=retailiq

# JWT secret (change for production)
JWT_SECRET=change_me_to_a_strong_value

# Client URL used for CORS
CLIENT_URL=http://localhost:5173
